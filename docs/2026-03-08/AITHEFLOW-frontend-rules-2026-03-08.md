# Аудит: AITHEFLOW (фронтенд) — соответствие правилам CLAUDE.md

**Дата:** 2026-03-08
**Путь:** /home/sasha/WORK/AITHEFLOW/src/
**Стек:** React 19 + TypeScript + Vite + Tailwind CSS v4 + Zustand
**CLAUDE.md:** найден

## Сводка

| Правило | Нарушений |
|---------|-----------|
| Иконки: только Lucide React | 1 |
| Цвета через CSS-переменные | 30+ |
| Не проглатывать ошибки | 0 |
| useShallow для filter/map в Zustand | 0 |
| Стриминг: plain text / markdown | 0 |
| Горячие клавиши через e.code | 1 |
| Хуки ДО раннего return null | 0 |
| Модули не знают друг о друга (сторы) | 0 (*) |
| Cross-store через getState() | 0 |

(*) Сторы импортируют друг друга, но используют ТОЛЬКО `.getState()` — не React-хуки. Это соответствует правилу.

---

## Нарушения

### [RUL-001] Инлайн-SVG в CliStatsSection (график)

- **Файл:** `src/components/settings/CliStatsSection.tsx:195-254`
- **Серьёзность:** СРЕДНЯЯ
- **Правило:** «Иконки: только Lucide React. Никаких CSS-иконок, никакого инлайн-SVG»
- **Описание:** Компонент содержит инлайн-SVG (`<svg>`, `<path>`, `<circle>`, `<line>`, `<linearGradient>`) для отрисовки графика расходов. Это не иконка, а data visualization — случай спорный. Однако формально правило запрещает «никакого инлайн-SVG» без оговорок.

---

### [RUL-002] Захардкоженные hex-цвета в layout.css

- **Файл:** `src/styles/layout.css:97`
- **Серьёзность:** СРЕДНЯЯ
- **Правило:** «Все цвета через CSS-переменные, никаких захардкоженных hex»
- **Описание:** `color: #66bb6a;` — захардкоженный зелёный цвет.

### [RUL-003] Захардкоженные hex-цвета в layout.css

- **Файл:** `src/styles/layout.css:102`
- **Серьёзность:** СРЕДНЯЯ
- **Правило:** «Все цвета через CSS-переменные, никаких захардкоженных hex»
- **Описание:** `color: #81c784;` — захардкоженный зелёный.

### [RUL-004] Захардкоженные hex-цвета в layout.css

- **Файл:** `src/styles/layout.css:119`
- **Серьёзность:** СРЕДНЯЯ
- **Правило:** «Все цвета через CSS-переменные, никаких захардкоженных hex»
- **Описание:** `background: #66bb6a;`

### [RUL-005] Захардкоженные hex-цвета в layout.css

- **Файл:** `src/styles/layout.css:180`
- **Серьёзность:** МЕЛКАЯ
- **Правило:** «Все цвета через CSS-переменные, никаких захардкоженных hex»
- **Описание:** `color: #fff;`

### [RUL-006] Захардкоженные hex-цвета в layout.css

- **Файл:** `src/styles/layout.css:1293`
- **Серьёзность:** СРЕДНЯЯ
- **Правило:** «Все цвета через CSS-переменные, никаких захардкоженных hex»
- **Описание:** `background: #4ade80;`

### [RUL-007] Захардкоженные hex-цвета в layout.css

- **Файл:** `src/styles/layout.css:1297`
- **Серьёзность:** СРЕДНЯЯ
- **Правило:** «Все цвета через CSS-переменные, никаких захардкоженных hex»
- **Описание:** `background: #f87171;`

### [RUL-008] Захардкоженные hex-цвета в layout.css

- **Файл:** `src/styles/layout.css:1310`
- **Серьёзность:** СРЕДНЯЯ
- **Правило:** «Все цвета через CSS-переменные, никаких захардкоженных hex»
- **Описание:** `background: #60a5fa;`

### [RUL-009] Захардкоженные hex-цвета в layout.css

- **Файл:** `src/styles/layout.css:1401`
- **Серьёзность:** СРЕДНЯЯ
- **Правило:** «Все цвета через CSS-переменные, никаких захардкоженных hex»
- **Описание:** `color: #f87171;`

### [RUL-010] Захардкоженные hex-цвета в layout.css

- **Файл:** `src/styles/layout.css:1433`
- **Серьёзность:** СРЕДНЯЯ
- **Правило:** «Все цвета через CSS-переменные, никаких захардкоженных hex»
- **Описание:** `background: #4ade80;`

### [RUL-011] Захардкоженные hex-цвета в layout.css

- **Файл:** `src/styles/layout.css:1484`
- **Серьёзность:** МЕЛКАЯ
- **Правило:** «Все цвета через CSS-переменные, никаких захардкоженных hex»
- **Описание:** `color: #fff;`

### [RUL-012] Захардкоженный hex в chat.css

- **Файл:** `src/styles/chat.css:638`
- **Серьёзность:** СРЕДНЯЯ
- **Правило:** «Все цвета через CSS-переменные, никаких захардкоженных hex»
- **Описание:** `color: #e74c3c;` — красный цвет без CSS-переменной.

### [RUL-013] Захардкоженный hex в settings.css

- **Файл:** `src/styles/settings.css:381`
- **Серьёзность:** МЕЛКАЯ
- **Правило:** «Все цвета через CSS-переменные, никаких захардкоженных hex»
- **Описание:** `background: #fff;`

### [RUL-014] Захардкоженный hex в settings.css

- **Файл:** `src/styles/settings.css:1060`
- **Серьёзность:** МЕЛКАЯ
- **Правило:** «Все цвета через CSS-переменные, никаких захардкоженных hex»
- **Описание:** `color: #fff;`

### [RUL-015] Захардкоженный hex в settings.css

- **Файл:** `src/styles/settings.css:1190`
- **Серьёзность:** МЕЛКАЯ
- **Правило:** «Все цвета через CSS-переменные, никаких захардкоженных hex»
- **Описание:** `color: #fff;`

### [RUL-016] Захардкоженные hex в code.css (блок подсветки синтаксиса)

- **Файл:** `src/styles/code.css:103-151`
- **Серьёзность:** СРЕДНЯЯ
- **Правило:** «Все цвета через CSS-переменные, никаких захардкоженных hex»
- **Описание:** Весь блок стилей для `.code-block .hljs` содержит ~20 захардкоженных hex-цветов (`#1e1c19`, `#282521`, `#3d3830`, `#665c54`, `#d6cdbf`, `#fb4934`, `#b8bb26`, `#d3869b`, `#928374`, `#fabd2f`, `#83a598`, `#8ec07c`, `#a89984` и др.). Часть из них совпадает со значениями CSS-переменных из theme.css, но используются напрямую через hex, а не через `var(--...)`.

### [RUL-017] Захардкоженный hex в fileviewer.css

- **Файл:** `src/styles/fileviewer.css:189`
- **Серьёзность:** МЕЛКАЯ
- **Правило:** «Все цвета через CSS-переменные, никаких захардкоженных hex»
- **Описание:** `color: #fff;`

### [RUL-018] Захардкоженные hex в cards.css

- **Файл:** `src/styles/cards.css:77, 107, 116, 191`
- **Серьёзность:** МЕЛКАЯ
- **Правило:** «Все цвета через CSS-переменные, никаких захардкоженных hex»
- **Описание:** Несколько `color: #fff;` без CSS-переменной.

### [RUL-019] Захардкоженные hex в TokensCard.tsx

- **Файл:** `src/components/dashboard/cards/TokensCard.tsx:13-16`
- **Серьёзность:** СРЕДНЯЯ
- **Правило:** «Все цвета через CSS-переменные, никаких захардкоженных hex»
- **Описание:** Объект `USAGE_COLORS` содержит прямые hex-значения: `"#4ade80"`, `"#f59e0b"`, `"#f87171"`, `"#888"`. Используются напрямую через `barColor` (строка 50, 70). Функция `getUsageColor` (строки 20-22) хотя бы использует `var(--color-..., fallback)`, но сам объект USAGE_COLORS применяется без переменных.

### [RUL-020] Захардкоженный hex в TelegramSection.tsx

- **Файл:** `src/components/settings/TelegramSection.tsx:107`
- **Серьёзность:** МЕЛКАЯ
- **Правило:** «Все цвета через CSS-переменные, никаких захардкоженных hex»
- **Описание:** Inline style `color: "var(--error, #e55)"` — fallback допустим, но правило не делает исключений.

### [RUL-021] Захардкоженные rgba() в CSS

- **Файл:** `src/styles/layout.css:96, 101` — `rgba(76, 175, 80, 0.12)`, `rgba(76, 175, 80, 0.2)`
- **Файл:** `src/styles/fileviewer.css:260, 268, 378, 382` — `rgba(184, 187, 38, 0.15)`, `rgba(251, 73, 52, 0.15)` и аналогичные
- **Файл:** `src/styles/settings.css:2383, 2388, 2457` — `rgba(80, 160, 80, 0.12)`, `rgba(200, 80, 80, 0.12)`
- **Файл:** `src/styles/cards.css:100` — `rgba(255, 255, 255, 0.8)`
- **Файл:** `src/styles/welcome.css:236` — `rgba(0, 0, 0, 0.4)`
- **Серьёзность:** СРЕДНЯЯ
- **Правило:** «Все цвета через CSS-переменные, никаких захардкоженных hex»
- **Описание:** Множественные `rgba()` с захардкоженными цветами. Цвета вроде `rgba(76, 175, 80, ...)` (Material Design green) не соответствуют палитре из theme.css и не обёрнуты в CSS-переменные.

**Примечание:** rgba() в shadow-переменных (`--shadow-sm`, `--shadow-md`, `--shadow-lg` в theme.css) и box-shadow — спорные, т.к. это тени с чёрным цветом для глубины, не «цвета интерфейса». Но формально это тоже нарушение.

---

### [RUL-022] Использование e.key вместо e.code для фильтрации модификаторов

- **Файл:** `src/components/settings/HotkeysSection.tsx:121`
- **Серьёзность:** МЕЛКАЯ
- **Правило:** «Горячие клавиши через e.code»
- **Описание:** `["Control", "Alt", "Shift", "Meta"].includes(e.key)` — используется `e.key` для проверки, является ли нажатая клавиша модификатором. Основная логика биндинга использует `e.code` корректно (строка 127-131), но фильтрация модификаторов идёт через `e.key`. Правильнее: `["ControlLeft","ControlRight","AltLeft","AltRight","ShiftLeft","ShiftRight","MetaLeft","MetaRight"].includes(e.code)`.

---

## Правила без нарушений

1. **Не проглатывать ошибки** — все `.catch()` содержат `console.error`. Пустых catch-блоков не найдено.
2. **useShallow для filter/map в Zustand** — все `useXxxStore(s => s.xxx.map/filter(...))` обёрнуты в `useShallow`. Остальные селекторы возвращают примитивы или функции (стабильные ссылки).
3. **Стриминг: plain text / markdown** — `AssistantMessage.tsx` при `isStreaming` использует `InlineMarkdown` (лёгкий regex-парсер), после завершения — `MarkdownRenderer`. Соответствует правилу.
4. **React хуки ДО раннего return null** — во всех проверенных компонентах (`ThinkingIndicator`, `GeneralSection`, `VoiceSection`, `TelegramSection`, `HooksSection`, `LanguageSection`, `SkillSection`, `InlineMarkdown`) хуки вызываются до `return null`.
5. **Каждый модуль = свой стор** — сторы не используют React-хуки друг из друга. Cross-store взаимодействие идёт через `.getState()` (проверено: `agentStore`, `chatService`, `conductorStore`, `translationStore`, `chatStreamHandler`, `fileViewerStore`, `projectStore`).
6. **Иконки из Lucide React** — все компоненты импортируют иконки из `lucide-react`. Сторонних иконочных библиотек не обнаружено. CSS-иконок через `::before`/`::after` с Unicode-символами нет (единственный `content: "\2022"` — типографский bullet, не иконка).
